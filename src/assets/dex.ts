/**
 * Archivo que se encarga del manejo del módulo Dex de la biblioteca de Pokémon Showdown.
 * Este archivo implementará diferentes funciones del módulo Dex que serán usadas en el resto del programa.
 *
 * Actualmente en proceso de construcción.
 */

import {Dex} from 'pokemon-showdown';

export const getAllPokemon = () => {
    //let pokemonList: string[];

    /*const pokemonList = Dex.mod('gen1').species.all();
    console.log(pokemonList);
    return pokemonList;*/

}
